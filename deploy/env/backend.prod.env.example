# Production backend environment template
# Copy on server as: /opt/asenovo/backend/.env.prod
# Then fill real secrets/hosts.

SPRING_PROFILES_ACTIVE=prod

# Database for docker-compose.prod.yml
POSTGRES_DB=asenovo
POSTGRES_USER=asenovo
POSTGRES_PASSWORD=asenovo767

# Security
JWT_SECRET=REPLACE_MIN_32_CHAR_SECRET
QR_SECRET_KEY=REPLACE_MIN_32_CHAR_SECRET

# CORS
CORS_ALLOWED_ORIGINS=https://*.asenovo.com

# Optional SMTP mail delivery
SMTP_HOST=email-smtp.us-east-1.amazonaws.com
SMTP_PORT=587
SES_SMTP_USERNAME=
SES_SMTP_PASSWORD=
MAIL_FROM=support@asenovo.com

# Resend email provider
EMAIL_PROVIDER=resend
RESEND_API_KEY=REPLACE_WITH_RESEND_API_KEY
EMAIL_FROM=noreply@mail.asenovo.com
EMAIL_ALLOWED_FROM_DOMAIN=mail.asenovo.com
EMAIL_TEST_RECIPIENT=sadik.ortaoglan@gmail.com

# Optional docs access
ASENOVO_DOCS_ENABLED=false

# Optional QR public base URL
QR_BASE_URL=https://api.asenovo.com

# Optional file storage
FILE_STORAGE_TYPE=local
FILE_STORAGE_LOCAL_DIR=/app/uploads

# Tenancy migration controls
TENANCY_MIGRATION_ENABLED=true
TENANCY_MIGRATION_INCLUDE_SHARED_SCHEMAS=true
TENANCY_MIGRATION_INCLUDE_DEDICATED_DBS=true
TENANCY_DEFAULT_SHARED_SCHEMA=public
TENANCY_FLYWAY_HISTORY_TABLE=flyway_schema_history
TENANCY_FLYWAY_BASELINE_ON_MIGRATE=true
TENANCY_FLYWAY_CONNECT_RETRIES=1

# Optional dedicated demo DB admin connection.
# Leave blank to reuse the main PostgreSQL server and create one separate database per demo tenant.
ASENOVO_MARKETING_ENVIRONMENT=prod
ASENOVO_MARKETING_MAIL_ENABLED=true
ASENOVO_MARKETING_APP_BASE_DOMAIN=asenovo.com
ASENOVO_MARKETING_APP_URL_TEMPLATE=https://{tenant}.asenovo.com/login
ASENOVO_MARKETING_DEMO_DB_ADMIN_URL=
ASENOVO_MARKETING_DEMO_DB_ADMIN_USERNAME=
ASENOVO_MARKETING_DEMO_DB_ADMIN_PASSWORD=
ASENOVO_MARKETING_DEMO_DB_NAME_PREFIX=asenovo_demo_
ASENOVO_MARKETING_DEMO_DB_TEMPLATE_NAME=asenovo_demo_template
