# Production frontend environment template
# Use this in frontend build pipeline/environment.

# Preferred API base for production
VITE_API_BASE_URL=https://api.asenovo.com/api

# Optional: app mode (if frontend uses it)
VITE_APP_ENV=production
